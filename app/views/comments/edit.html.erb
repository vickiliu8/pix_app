<h1> edit your comment </h1>

<%= form_for @comment, :url => { :action => "update"} do |f| %>
  
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>